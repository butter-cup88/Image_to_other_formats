Use the following commands to install packages used in this code, on Windows
pip install Pillow

pip install pillow-heif

pip install ttkbootstrap

***** CODE WILL NOT RUN IF ANY OF THE PACKAGES IS MISSING *****
